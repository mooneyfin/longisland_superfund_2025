# ── OS / editor ───────────────────────────────────────────────
.DS_Store

# ── R ─────────────────────────────────────────────────────────
.Rhistory
.RData
.Rapp.history
.Rproj.user/
rosm.cache/

# knitr / rmarkdown intermediates (the .Rmd source and rendered .html are kept)
*.knit.md
*.utf8.md
*.tex

# ── ArcGIS lock + spatial-index sidecars (regenerable) ────────
*.lock
*.sbn
*.sbx
*.pitemx

# ── Manuscripts, article, and other local-only material ───────
manuscript/
*.docx
*.pdf

# ── Large / national source data (kept local; see README "Data sources") ──
Data/
01_data/npl_boundaries/
01_data/nys_civil_boundaries.gdb/
01_data/census_tracts_2020/
